import { INavItem } from "../types";

export const menuItems: INavItem[] = [
	{ key: '1', label: 'CURRENCY CONVERTER', path: '/' },
	{ key: '2', label: 'VIEW CONVERSION HISTORY', path: '/history' }
]
